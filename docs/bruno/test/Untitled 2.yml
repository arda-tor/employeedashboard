info:
  name: Untitled 2
  type: http

http:
  method: GET
  url: http://localhost:8000/api/employees
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​